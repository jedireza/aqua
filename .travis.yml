sudo: false
language: node_js
node_js:
  - "4.0"
  - "5.0"
services:
  - mongodb
env:
  - NODE_ENV=test && CXX=g++-4.8
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.8
before_script:
  - npm install -g npm@'>=2.13.0' && npm update npm && npm run setup
